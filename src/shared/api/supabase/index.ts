/**
 * Supabase Client Exports
 *
 * - `useSupabaseClient` → Client Components (browser)
 * - `createSupabaseServerClient` → Server Components, Server Actions, Route Handlers
 * - `Database`, `Tables`, `TablesInsert`, `TablesUpdate` → TypeScript types
 */

// Types (podem ser importados em qualquer lugar)
export type {
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Json,
} from "./database.types";
